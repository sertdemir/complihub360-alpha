---
status: waiting
assignee: architecture-master, repo-master
---
# TICKET-001: Dashboard Core Setup & Routing

Scaffold `dashboard` and `partner-dashboard` directories in `src/pages`. Create the `DashboardShell.tsx` (TopNav & SideNav). Update `App.tsx` routing for both dashboard types (`/:locale/dashboard/...` and `/:locale/partner-dashboard/...`). Create `useDashboardStore.ts` (Zustand) to manage mock session and lead data.

## Agent Result / Execution

## Agent Audit Log
- [TBD] **Task-Master**: Ticket Created (Status: waiting)
