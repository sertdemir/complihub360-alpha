---
title: "Backend Epic & Audit Parser"
assignee: "Repo-Engineer"
status: "todo"
epic: "EPIC-9800"
---

# Backend API Updates for Orchestration

To support explicit multi-agent workflow tracking, modify the Node.js dev board backend.

## Requirements

1. Add `waiting` to the list of acceptable `.tickets` statuses and `ensureDirs`.
2. Parse the Frontmatter `epic: "[EPIC-ID]"` if it exists, and pass it in the JSON payload of `/api/tickets`.
3. Extract `## Agent Audit Log` from the bottom of the Markdown content. Return it as a dedicated structured array or string in the API response so the frontend can easily render a timeline.

## Agent Audit Log

- [2026-02-28T01:25:00Z] **Task-Master**: Created ticket and assigned to Repo-Engineer. (Status: todo)
