# Quickstart: 30-Second Init

To create the next boundary slice application immediately:

```bash
# Start your scaffolding
npm run create:app

# Install dependencies and sync workspaces
npm install

# Build everything
npm run build
```

Head over to `docs/TEMPLATE.md` to see exactly how your app architecture connects to the `packages/*` registry system.
