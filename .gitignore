
.DS_Store
**/.DS_Store


# dependencies
node_modules/
**/node_modules/

# build outputs
**/dist/
**/.vite/
.DS_Store
node_modules/
**/node_modules/
**/.vite/
**/dist/

# typescript cache
*.tsbuildinfo
**/*.tsbuildinfo
# .env should never be committed to git
.env
.env.*
# …except the templates. `.env.*` swallowed these silently, so every
# .env.example in the repo was untracked without anyone noticing. Exact-name
# negation only — nothing with a real secret matches it.
!.env.example

# Supabase local state
supabase/.temp
supabase/.branch

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm-cache/

*storybook.log
storybook-static

# Python virtual environments
*-env/
*-env2/
test-env/
.notebooklm-mcp-env/
venv/
.venv/
__pycache__/
*.py[cod]

# Session artifacts (screenshots land in repo root during design verification)
/*.png
.playwright-mcp/
.DS_Store

# Claude workspace internals — only skills + launch config are project assets
.claude/*
!.claude/skills/
!.claude/launch.json

# Accidental early-session scaffold (5 files, superseded by the real repo layout)
apps/complihub360-alpha/
