{
    "name": "@complihub/compliance-engine",
    "version": "0.1.0",
    "description": "Deterministic country-aware compliance risk engine",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -b",
        "typecheck": "tsc -b --noEmit",
        "test": "vitest run"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "vitest": "^1.0.0"
    }
}