{
    "name": "@complihub/policy-engine",
    "version": "0.1.0",
    "description": "Deterministic Multi-Tenant Policy Evaluation Engine",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -b",
        "clean": "rm -rf dist tsconfig.tsbuildinfo",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@complihub/agent-core": "workspace:*"
    }
}